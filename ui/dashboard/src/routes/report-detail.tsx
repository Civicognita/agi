/**
 * Report detail route — shows full report for a single COA request.
 */

import { ReportDetail } from "@/components/ReportDetail.js";

export default function ReportDetailPage() {
  return <ReportDetail />;
}
