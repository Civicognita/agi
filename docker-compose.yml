services:
  gateway:
    build: .
    ports:
      - "3100:3100"
    volumes:
      - aionima-data:/app/data
      - ./gateway.json:/app/gateway.json:ro
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  aionima-data:
